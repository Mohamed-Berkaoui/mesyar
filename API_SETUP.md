# المساير Chat API Setup (Mesayir Chat API)

## مشكلة الاتصال بـ API في التطوير المحلي

إذا كنت تستقبل رسالة خطأ: **"لم يتم العثور على خدمة الـ API"**

### السبب
- عند تشغيل `npm run dev`، Vite لا يقوم بتشغيل serverless functions من مجلد `/api`
- الـ API يعمل **فقط** على Vercel بعد النشر

---

## الحل 1: نشر على Vercel (الطريقة الموصى بها) ✅

### خطوات النشر:

1. **تثبيت Vercel CLI:**
```bash
npm install -g vercel
```

2. **تسجيل الدخول:**
```bash
vercel login
```

3. **نشر المشروع:**
```bash
vercel
```

4. **إضافة متغير البيئة في Vercel:**
   - اذهب إلى [Vercel Dashboard](https://vercel.com/dashboard)
   - اختر مشروعك
   - انقر على **Settings** → **Environment Variables**
   - أضف: `OPENAI_API_KEY` = `sk-proj-...` (مفتاحك)
   - انقر **Save**

5. **إعادة النشر:**
```bash
vercel --prod
```

بعد ذلك، الـ Chat AI سيعمل بشكل صحيح ✅

---

## الحل 2: اختبار محلي مع Vercel CLI

إذا أردت اختبار الـ API محلياً قبل النشر:

```bash
# تثبيت Vercel CLI
npm install -g vercel

# تشغيل البيئة المحلية لـ Vercel
vercel dev
```

سيشغل السرفر على `http://localhost:3000` مع دعم serverless functions.

---

## التحقق من المفتاح

تأكد أن مفتاح OpenAI:
- ✅ نسخت الرقم كاملاً بدون مسافات
- ✅ يبدأ بـ `sk-proj-` أو `sk-`
- ✅ لم ينته صلاحيته من OpenAI Dashboard
- ✅ يحتوي على صلاحيات `chat.completions`

---

## معلومات إضافية

### الملفات الأساسية:
- **`api/chat.ts`** - Serverless function لـ OpenAI API
- **`src/components/ChatBubble.tsx`** - Chat UI component
- **`.env`** - متغيرات البيئة (مع .gitignore، لا تنشرها)

### الـ API Flow:
```
ChatBubble (React) 
  ↓
fetch('/api/chat')
  ↓
api/chat.ts (Vercel Serverless)
  ↓
OpenAI API (GPT-4o-mini)
  ↓
Response back to ChatBubble
```

---

## رسائل الخطأ الشائعة

| الرسالة | السبب | الحل |
|--------|------|------|
| تم العثور على الخدمة | API endpoint غير موجود | نشر على Vercel أو استخدم `vercel dev` |
| خطأ في المفتاح | مفتاح OpenAI غير صحيح | تحقق من المفتاح في `.env` و Vercel settings |
| تجاوزت الحد الأقصى | 10 رسائل كل 10 دقائق | انتظر قبل إرسال رسائل جديدة |

---

**جرّب الآن وأخبرني إذا استمرت المشكلة!** 🚀
